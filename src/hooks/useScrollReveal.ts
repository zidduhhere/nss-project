// Simple fade-in animation hook - no longer needed as we use CSS animations
// Keeping this file for backwards compatibility but it does nothing

export function useScrollReveal() {
  // No-op - animations now handled by CSS
}

export default useScrollReveal;
